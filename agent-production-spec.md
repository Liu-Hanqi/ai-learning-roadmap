# 生产级 Agent 项目规格:从 Demo 到中小企业可用

> 适用范围:路线图第 13–26 周的 Agent + 模型服务层综合项目。
> 核心立场:**生产级不是"更聪明",而是"可度量、可兜底、可运维"。**
> 本文的"对标物"不是某个产品,而是一组可验证的工程指标。

---

## 0. 先定义边界:中小企业要什么,不要什么

| 中小企业真实需求 | 中小企业不在乎的 |
|---|---|
| 能接进现有系统(工单、CRM、IM、内部文档) | 百万 QPS 水平扩展 |
| 成本可预测,每月账单不惊悚 | 多地域容灾 |
| 出错时能兜底,不会静默胡说 | 99.99% 可用性 |
| 部署简单,没有专职 ML 工程师也能维护 | 自研推理框架 |
| 数据不外流 / 权限隔离 | 模型榜单排名 |
| 出问题能查(谁、何时、为什么) | 多智能体自主协作的花活 |

**一个反直觉但重要的结论**:中小企业生产环境里,大多数成功落地的东西**不是自主 Agent,而是"确定性工作流 + LLM 节点"**——流程由代码编排,LLM 只负责其中需要理解和生成的环节。完全自主的 Agent 只在探索型任务(如代码修复)里才划算。

自检问题:如果把你项目里的 LLM 换成"一个会调用 API 的实习生",这套流程还成立吗?如果成立,说明你的架构是稳的。

---

## 1. Demo 到生产的八个断层

这是最核心的一张表。逐条对照,任何一条没过,项目就还是 demo。

| # | 维度 | Demo 状态 | 生产状态 | 如何验收 |
|---|---|---|---|---|
| 1 | 任务成功率 | 演示时能跑通 | 固定评测集上成功率达标 | 自动评测脚本,可复现 |
| 2 | 错误行为 | 编一个像样的答案 | **显式失败**,不静默错误 | 故障注入测试 |
| 3 | 延迟 | 没测过 | TTFT、P95 有预算且达标 | 压测报告 |
| 4 | 成本 | 没算过 | 每任务成本有上限,月度可预测 | 埋点统计 |
| 5 | 可观测 | 翻日志 | 全链路 trace,可根因分析 | 抽查任意一次回答 |
| 6 | 权限与安全 | 无 | 按角色过滤数据,防注入 | 越权测试用例 |
| 7 | 部署 | 本机跑通 | 干净机器一键部署,可回滚 | 重置环境验证 |
| 8 | 回归 | 改完手动试 | CI 跑评测集,防止变差 | 流水线门禁 |

---

## 2. 可验收的指标(这就是"对标")

数字是参考值,按你的任务性质调整,但**必须写下来并测出来**。

### 2.1 质量

| 指标 | 参考目标 | 说明 |
|---|---|---|
| 任务成功率 | ≥ 80%(有审查环节可放宽到 70%) | 在自建 eval 集上,按任务级判定 |
| 忠实度 | ≥ 95% | 回答必须能追溯到检索到的证据,禁止编造 |
| 正确转人工率 | 100% | 命中"不确定/超范围"条件时必须转人工,而不是硬答 |
| 评测集规模 | ≥ 50 条真实任务 | 从真实使用中沉淀,不能靠自己编 |

**关键原则:宁可显式失败,不可静默幻觉。** 这一条是 demo 与生产的最大分水岭。

### 2.2 性能

| 指标 | 参考目标 |
|---|---|
| 首字延迟 TTFT | < 2s |
| 端到端 P95 | 按任务定,单轮问答 < 10s,多步任务 < 60s |
| 并发 | 中小企业通常 5–20 并发,不必追高 |
| 超时处理 | 每个工具调用和模型调用都有超时与重试上限 |

### 2.3 成本

- 记录每次请求的 token 用量与折算成本
- 设定每任务成本上限(如 < ¥0.5),超限告警
- 缓存命中率、小模型分流率作为优化指标
- 月度成本可预测:能画出"请求量 → 费用"的直线

### 2.4 可靠性

| 故障 | 必须有处理 |
|---|---|
| 模型 API 超时/限流 | 重试 + 指数退避 + 降级到备用模型 |
| 工具调用失败 | 有界重试,失败后进入明确分支 |
| 检索为空 | 走"没找到资料,不确定"路径,不硬答 |
| 服务重启 | 有状态任务可恢复或明确失败,不产生半截副作用 |
| 副作用幂等 | 重复请求不重复执行(如重复建单) |

---

## 3. 必须建的三套基础设施

这三样是"生产级"的实物证据,也是面试/作品集里最有说服力的部分。

### 3.1 评测系统(最重要,最先建)

```
评测集(≥50 条真实任务,含标准答案要点)
   ↓
自动评测脚本(命中率 / 忠实度 / 任务成功判定)
   ↓
CI 集成:每次改 prompt、换模型、改检索 → 自动跑 → 分数下降则挡回
```

**为什么它最早建**:没有评测,后面所有优化都是凭感觉;有了评测,你才能回答"这次改动到底有没有变好"——这是工程师和爱好者最本质的区别。

参考方法:SWE-bench 的思路(固定任务集 + 可自动判定的验收标准)。工具可选 Ragas(检索质量)、自建脚本(任务成功率)。

### 3.2 可观测系统

每次运行必须能回答这四个问题:

1. 用户问了什么,系统的最终回答是什么?
2. 检索到了哪些片段,为什么选它们?
3. 调用了哪些工具,参数和返回是什么?
4. 花了多少 token、多少钱、多长时间?在哪一步失败?

落地:全链路 trace(可选 Langfuse / LangSmith,也可自建),加上错误分类仪表盘。**没有 trace,线上问题无法定位,运维就无从谈起。**

### 3.3 权限与安全层

| 风险 | 措施 |
|---|---|
| 用户越权读到不该读的文档 | 检索层强制按角色过滤,不是靠 prompt 说"请不要回答" |
| Prompt injection(检索内容里藏指令) | 检索内容视为不可信数据,与指令隔离;输出校验 |
| 密钥泄露 | 密钥走环境变量/密钥管理,不入库不入日志 |
| 敏感信息外流 | 明确数据边界:哪些走本地模型,哪些可走 API;脱敏 |
| 审计 | 记录谁在何时让系统做了什么(尤其有副作用时) |

**权限必须做在检索层,不能做在 prompt 层。** 靠提示词让模型"不要泄露"是最常见的伪安全设计。

---

## 4. 人机边界(决定可接受错误率)

生产环境必须明确回答:"哪些事系统自动做,哪些必须人确认?"

| 动作类型 | 建议策略 |
|---|---|
| 只读查询、信息检索 | 全自动 |
| 生成草稿、建议 | 全自动 + 人工采纳 |
| 有副作用的操作(建单、发信、改数据) | **必须人确认**,或限定到白名单动作 |
| 高不确定性/超范围 | 强制转人工 |

**这个边界定义清楚了,可接受的错误率才有意义**——因为错误由人兜底,风险可控。反过来,让 Agent 直接执行不可逆操作,再高的成功率也不叫生产可用。

---

## 5. 交付物长什么样

最终仓库应该包含(这就是你的"生产级证据"):

```
agent-project/
├── README.md              # 架构图 + SLO 表 + 部署步骤 + 已知限制
├── docker-compose.yml     # 一键起服务(应用 + 向量库 + 观测)
├── .env.example           # 配置外置,密钥不入库
├── src/
│   ├── workflow/          # 确定性编排(主流程)
│   ├── agent/             # 需要探索的环节才用 agent
│   ├── retrieval/         # 检索 + 权限过滤
│   ├── gateway/           # 缓存、限流、路由、降级
│   └── observability/     # trace 埋点
├── eval/
│   ├── dataset.jsonl      # ≥50 条真实任务
│   ├── run_eval.py        # 自动评测
│   └── report.md          # 最近一次评测结果
├── tests/
│   ├── test_permissions.py   # 越权用例(必须有)
│   └── test_failures.py      # 故障注入
└── docs/
    ├── runbook.md         # 出问题怎么办
    └── decisions.md       # 关键技术决策与理由(ADR)
```

**README 里的 SLO 表是整份作品的门面**——它直接宣告"我知道生产意味着什么,并且我做到了"。

---

## 6. 反模式:会直接暴露"这还是 demo"

- ❌ 多智能体自主协作(对中小企业场景几乎总是过度设计)
- ❌ 一上来就微调(先用 prompt + RAG 打到瓶颈再说)
- ❌ 没有评测就开始调 prompt(凭感觉优化,无法证明变好)
- ❌ 靠 prompt 做权限控制
- ❌ "可观测性以后再补"(补不上的,事后无法回溯历史)
- ❌ 让 Agent 直接执行不可逆操作
- ❌ 只展示成功案例,不展示失败模式与兜底路径

---

## 7. 与 26 周路线图的对应

| 周次 | 产出 | 对应的生产要求 |
|---|---|---|
| 13–15 | RAG + 手写 Agent + 框架版 | 功能实现(仍是 demo 阶段) |
| 16 | Agent 评测 | **第 1 条断层:任务成功率** |
| 17–18 | vLLM 部署 + 压测 | 第 3 条断层:延迟 |
| 19–21 | 网关:缓存、限流、路由、降级 | 第 4、2 条断层:成本与可靠性 |
| 22–23 | 端到端打通与调优 | 集成完整性 |
| 24 | 监控与成本 | 第 5 条断层:可观测 |
| 25 | 复盘文章 | 展示失败模式与人机边界 |
| 26 | 终复盘 | 自我评估:八条断层过了几条 |

**权限层、评测集、故障注入测试**这三样在路线图原文里没写足,需要额外补进去——它们是生产级的硬门槛。

---

## 8. 诚实的自我定位

一个人几周的业余项目,交付的是**生产级参考实现**,不是"已在企业跑着的系统"。区别在于:

- ✅ 可以证明:架构、评测、故障处理、可观测性、部署方案都到位
- ❌ 无法证明:大规模流量下的表现、长期运维成本、真实用户的接受度

这个定位在面试里反而是加分项——说清"我做到了什么、什么还没验证"的人,比声称"企业级"的人可信得多。**知道自己系统边界在哪,本身就是生产意识的一部分。**
