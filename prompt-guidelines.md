# Prompt 工程:两家官方指南笔记 + 我的 10 条准则

> 第 2 周任务产出。
> 来源说明:OpenAI 官方指南取自其官方 Cookbook 仓库的 `examples/data/oai_docs/prompt-engineering.txt`(平台文档站有反爬拦截);
> Anthropic 部分取自其官方交互式教程仓库 `anthropics/prompt-eng-interactive-tutorial`(文档站有区域限制)。
> 两家均为官方一手材料,非二手转述。

---

## 0. 两份指南的结构对照

| OpenAI:六策略 | Anthropic:九章 |
|---|---|
| 1. 写清晰的指令 | 1. 基本 Prompt 结构(system / user / assistant) |
| 2. 提供参考文本 | 2. 清晰直接(把模型当成第一天上岗的新人) |
| 3. 把复杂任务拆成简单子任务 | 3. 角色提示 |
| 4. 给模型"思考"的时间 | 4. 分隔数据与指令(XML 标签) |
| 5. 使用外部工具 | 5. 格式化输出与"替 Claude 说话"(prefill) |
| 6. 系统化地测试改动 | 6. 让模型一步步思考 |
| | 7. 使用示例(few-shot) |
| | 8. 避免幻觉 |
| | 9. 从零构建复杂 Prompt |

**两家的框架不同,但内核高度重合。** OpenAI 是"策略清单"式,偏功能;Anthropic 是"教程递进"式,偏机制。

---

## 1. 两家共识(八条交集,这是最该记住的)

1. **清晰、直接**——模型的默认状态是"没有上下文",不写清楚它就猜
2. **给角色和上下文**——system prompt 是放规则和身份的地方
3. **分隔指令与数据**——用标记把"我说的话"和"我给你的材料"分开
4. **用示例说明**——展示比描述有效得多(few-shot)
5. **需要推理的任务要分步**——让模型先想再答
6. **提供参考文本并要求引用**——对抗幻觉的主要手段
7. **明确指定输出格式**——不要指望模型猜你想要的形状
8. **系统化测试**——Prompt 改动必须用测试集验证,不能靠感觉

---

## 2. 两家的差异(值得注意)

| 维度 | OpenAI 更强调 | Anthropic 更强调 |
|---|---|---|
| 分隔符 | 任意分隔符(`"""`、`###`) | **明确推荐 XML 标签**,称其模型专门训练过 |
| 输出控制 | 结构化输出 / JSON mode | **Prefill**:"替模型说第一句",强制它从某处续写 |
| 思考 | 给模型思考时间 | **"思考只有说出来才算数"**——要求思考外显,不能只要答案 |
| 工程化 | **系统化评测(evals)** 单列一条策略 | 放在附录(Empirical Performance Evaluations) |
| 工具 | 外部工具单列一条策略 | 放在附录 |

**三条最实用的具体技巧:**

1. **XML 标签分隔**(Anthropic):`<instructions>...</instructions>` + `<data>...</data>`,解决"模型分不清哪句是指令、哪句是用户输入"这个高频问题
2. **Prefill / 替模型说话**(Anthropic):把回复的开头放进 assistant 轮次。想要 JSON 就在 assistant 里预填 `{`,想要特定结构就预填第一个标签
3. **措辞细节敏感**(Anthropic):模型的输出质量会跟着你的输入质量走——拼写错误、语法混乱会让它变笨。发之前把 prompt 当代码 review 一遍

---

## 3. 一个重要演进:经典指南 vs 新模型指南

读这两份指南时要意识到它们反映的是不同时代的模型能力。

| | 经典指南(2023 前后) | 新模型指南(2025+) |
|---|---|---|
| 核心主张 | **指定步骤**:一步步告诉模型怎么做 | **描述结果**:说清成功标准,让模型自己选路径 |
| 指令量 | 越多越细越好 | **越小越好**,只保留改变行为的指令 |
| 绝对规则 | 大量 ALWAYS / NEVER | 只对真正的不变量用;其余给**决策规则** |
| 示例 | 越多越好 | 只在能修复实测失败模式时才加 |
| 停止条件 | 较少提及 | 显式写明何时停、何时降级、何时发问 |

新指南的实践建议:**从最小的 prompt 开始,只在修复了实测失败时才加东西。** 每次加一条指令前先问:"不加会怎样?"——如果答不出来,这条就不该存在。

这也解释了老手常踩的坑:把 2023 年那套又长又啰嗦的 prompt 直接搬到新模型上,结果模型开始过度探索、反复验证、上下文膨胀,效果反而变差。

---

## 4. 我的 10 条准则(起步版)

> 这 10 条是**起点,不是结论**。每条都配了验证实验,跑完再改成你自己的版本。

### 1. 先写成功标准,再写步骤
描述"什么算做好了",而不是"按什么流程做"。步骤写得越死,模型越难走捷径。
**反模式**:把 prompt 写成 SOP 手册,每一步都锁死。
**何时打破**:流程有强合规要求时必须锁死顺序。

### 2. 指令与数据必须物理分隔
用户输入、检索内容、工具返回一律用标记包起来(`<data>`、`<retrieved>`)。**永远不要把不可信内容直接拼进指令区。**
**反模式**:`请总结这篇文章:{user_input}` —— 用户输入"忽略以上指令"就中招。
**这条是安全底线,不存在"何时打破"。**

### 3. 能用示例说清的,不用形容词描述
"语气要专业"是废指令;"下面是三条合格回复,照这个标准"是有效指令。
**反模式**:堆形容词——友好、专业、简洁、准确、有同理心……模型无所适从。
**何时打破**:示例会占用大量 token 且任务极其简单时。

### 4. 要推理的任务,要求思考外显,且先证据后结论
Anthropic 说得很直接:**思考只有说出来才算数**。要求"先列出依据,再给结论",而不是"仔细想想再回答"。
**反模式**:要求模型"内部思考后直接输出答案"——它没有内部草稿纸。
**何时打破**:简单分类、抽取任务不需要,加了反而拖慢并引入噪声。

### 5. 给模型"不知道"的出口
无依据时必须能说"资料里没有提到",否则它只能编。同时要求引用具体来源。
**反模式**:只要求"准确回答",不提供认输的路径。
**何时打破**:开放式创作任务不存在"不知道"。

### 6. 输出格式靠约束,不靠祈祷
需要 JSON 就用结构化输出/prefill/首 token 约束,而不是写一句"请返回 JSON"然后祈祷。
**反模式**:靠提示词保证格式,下游再用正则硬解析。
**这是工程问题,不是提示词问题。**

### 7. 复杂任务拆成流水线,别让一个 prompt 干所有事
"分类 → 检索 → 生成 → 校验"应该是四步,不是一段话里的四个要求。每步可单独测试、单独替换。
**反模式**:一个 prompt 里塞进十个要求,出错时无法定位是哪一步。
**何时打破**:任务确实原子且短。

### 8. Prompt 是代码:版本化、有测试、改动跑评测
改 prompt 前先有测试集。没有评测的优化是感觉,不是工程。
**反模式**:凭"感觉这次好了"就上线。
**这条直接决定你是工程师还是爱好者。**

### 9. 少即是多,删掉不改变行为的指令
新模型对冗余指令敏感。定期做"减法实验":删掉一条,看评测是否下降。
**反模式**:只增不减,prompt 滚成上千行。
**何时打破**:安全与合规约束永远不删。

### 10. 每条 prompt 都要有兜底路径
明确写出:不确定时怎么办、工具失败时怎么办、检索为空时怎么办。**生产环境里,失败路径比成功路径更重要。**
**反模式**:只描述理想流程,异常情况全靠模型临场发挥。
**这条和第 5 条的区别**:第 5 条是"允许说不知道",这条是"必须定义接下来做什么"。

---

## 5. 验证实验(把准则变成你自己的)

每条准则用一个小实验验证,别信我的话,信你的数据:

| # | 实验 | 观察指标 |
|---|---|---|
| 2 | 同一任务:裸拼输入 vs `<data>` 包裹,各跑 20 次,中间夹带一句注入文本 | 注入成功率 |
| 3 | 形容词描述 vs 3 个示例 | 输出合格率、方差 |
| 4 | "直接给答案" vs "先列依据再给答案" | 复杂推理题正确率 |
| 5 | 有/无"不知道"出口,喂 10 个无答案问题 | 编造率 |
| 6 | "请返回 JSON" vs 结构化输出约束,各跑 50 次 | 解析失败率 |
| 9 | 全量 prompt vs 删掉一半指令 | 评测分数是否下降 |

**这六个实验跑完,你会得到属于自己的准则——比任何指南都有说服力。**

---

## 6. 上线前速查清单

- [ ] 成功标准写清楚了吗?
- [ ] 不可信输入被标记隔开了吗?
- [ ] 有没有至少一个示例?
- [ ] 需要推理的任务,思考路径外显了吗?
- [ ] 有"不知道"的出口吗?
- [ ] 输出格式是约束出来的,还是祈祷出来的?
- [ ] 失败路径定义了吗(超时、空检索、工具失败)?
- [ ] 有没有测试集?改动跑过评测吗?
- [ ] 有没有可以删掉的冗余指令?
- [ ] 敏感操作是否要求人工确认?

---

## 7. 参考来源

- OpenAI,《Prompt engineering》六策略官方指南(官方 Cookbook 仓库文本版)
- OpenAI,《Model guidance — Prompting best practices》(新模型 prompt 实践)
- Anthropic,《Prompt Engineering Interactive Tutorial》九章(官方教程仓库)
