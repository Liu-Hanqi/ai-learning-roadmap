# 第 1 周自测记录与错题本

> 用途:记录回答、批改与该回炉的点。**错题比笔记更有复习价值。**
> 复习方式:每隔一两周只看"需回炉"小节,能答顺了就划掉。

---

## 第一轮:文本建模基础(7 题)

### 1. 为什么 exp(W) 会收敛到 bigram 计数表?
**我的回答**:训练中损失函数约束,梯度下降重新挑中参数,让模拟值拟合真实值,从随机分布走向真实分布。
**批改**:方向对,表述偏模糊。更锐利的说法——**两条路在爬同一座山,山顶唯一**:交叉熵就是负对数似然,而 bigram 条件下负对数似然的理论最优解恰好是归一化计数表(最大似然估计)。不是"碰巧拟合得像",而是**最优解相同**。
**状态**:✅ 已修正理解。

### 2. one-hot 乘矩阵为什么等价于查表?
**我的回答**:理所当然,每个字符的位置就对应一行。
**批改**:直觉正确,补形式化——one-hot 向量 e_i 只有第 i 位是 1,`e_i @ W` 展开后只有第 i 行被"选中"。工程上直接索引更快,但**理解成矩阵乘法很重要**,因为矩阵乘可微,embedding 才能进计算图跟着梯度更新。
**状态**:✅ 已修正理解。

### 3. 为什么"好"和"坏"的 embedding 离得近?
**我的回答**:它们出现的位置/上下文非常相似。这说明 embedding 很难区分反义词。
**批改**:正确。这就是**分布假说**——词的表示由上下文分布定义。静态 embedding 编码的是"用法分布",不含极性信息。后续 ELMo/BERT/GPT 用上下文相关的动态表示来补这个洞。
**状态**:✅ 正确。

### 4. tanh 死亡怎么诊断、怎么治?
**我的回答**:pre-activation 离 0 太远,输出大量 ±1;需要在初始化时做额外处理。
**批改**:正确。补量化标准——统计 `(|h| > 0.99)` 的比例,过半就危险。治疗:初始化按增益缩放(tanh 用 5/3/√fan_in),或上 BatchNorm。
**状态**:✅ 正确。

### 5. BatchNorm 训练和推理为什么不一致?会引起什么 bug?
**我的回答**:以深度学习经验看,是过拟合问题。
**批改**:❌ **答错,且是 CV 经验误导的地方**。真正的机制是**统计量来源不同**:训练用当前 batch 的均值方差,推理用训练期滑动平均攒下的 running mean/var。后果是激活分布漂移,makemore 里表现为推理时输出退化成复读机。附带一个隐蔽性质:训练时样本输出依赖同 batch 的其它样本。
**状态**:❌ **需回炉**——这是理解 LayerNorm 的台阶(Transformer 用 LayerNorm 正是为了绕开 batch 依赖)。

### 6. 为什么反复看 test loss 等于作弊?
**我的回答**:属于过拟合问题。
**批改**:沾边,机制要说准——不是模型参数过拟合,而是**你的决策过拟合**。每次根据 test loss 调超参/选 checkpoint,test 集信息就通过你的手漏进模型;看十次,test 实际上变成了 dev,报出的泛化性能虚高。
**状态**:⚠️ 基本正确,表述需精确。

### 7. 从 MLP 到 Transformer,换掉了什么?没换的是什么?
**我的回答**:注意力组件!
**批改**:正确。换掉的是上下文建模组件(固定窗口拼接 + MLP,以及中间的 RNN),换成 attention。没换的是整条流水线:embedding → 网络 → softmax → 交叉熵 → 预测下一个 token。
**状态**:✅ 正确。

**第一轮得分:5.5 / 7**

---

## 第二轮:预训练 → SFT → RLHF/RLVR(10 题)

### 1. base model 和 instruct model 的本质区别?
**我的回答**:前者是文本模拟器,拟合文本数据;后者激活了判断正确答案的能力。
**批改**:前半对,后半要修——SFT 教的是**行为格式**,不是**正确性判断**。instruct model 照样会一本正经胡说。"答非所问"的机制说对了:base model 把 prompt 当作**待续写的文本片段**,模拟"包含这个问题的网页接下来会写什么",而不是回应提问者。
**状态**:⚠️ 需区分"扮演助手"和"答对"。

### 2. BPE 的 50000 次合并怎么决定?为什么是 50257?
**我的回答**:大概率是性能效率的权衡,作用是找到合适的词组。50257 = 50000 + 256 + 特殊符号。
**批改**:正确。精确表述:词表越大序列越短(省算力)但 embedding 与输出 softmax 越大(费参数),50000 是 GPT-2 选的平衡点,不是理论最优。
**状态**:✅ 正确。

### 3. 字节级编码为什么没有 OOV?代价是什么?
**我的回答**:所有 Unicode 字符底层就是 256 个字符;代价是底层序列变长、要做无意义的合并。
**批改**:正确。精确一点:Unicode 经 UTF-8 编码后就是字节序列,字节只有 256 种,故任何文本都能编码。代价是序列变长、多字节字符被拆成无独立语义的碎片(中文 token 效率低的根源)。
**状态**:✅ 正确。

### 4. SFT 为什么只对 assistant 部分算 loss?
**我的回答**:本质是问答,目的是让模型学会回答;带上 user 会激活它提问的能力。
**批改**:正确。更精确:算 user 会让模型花容量去建模"用户会怎么说话"这个推理时用不上的分布,并可能错误续写用户的话。
**状态**:✅ 正确。

### 5. LIMA 的 1000 条说明了什么?
**我的回答**:SFT 是在激活模型本身的能力。
**批改**:正确。"激活"抓住了本质:SFT 无法注入预训练中不存在的知识。
**状态**:✅ 正确。

### 6. 为什么 RLHF 需要 KL 惩罚?没有会怎样?
**我的回答**:让模型专注奖励,生成完全飘逸的答案。(提出疑问:有例子吗?)
**批改**:结论对,补两个经典例子:
- **CoastRunners 赛艇**:奖励"名次",AI 发现原地转圈刷分道具得分更高,于是永不冲线——优化的是奖励,不是目标
- **LLM 讨好化**:奖励模型学到"自信、篇幅长、热情"得高分,无 KL 约束时 PPO 会把该倾向放大,输出变成"这是绝妙的问题!你完全正确!以下是极其详尽的答案……"——内容空洞却句句踩高分特征,人类翻白眼而 RM 打满分

KL 的作用:**信任奖励模型,但只信任一点点**。
**状态**:✅ 已补例子。

### 7. 奖励模型和人类偏好是什么关系?
**我的回答**:奖励模型根据人类标注训练,是人类偏好的模拟网络,所以只是代理。
**批改**:正确。术语补充:这是 **Goodhart 定律**的实例——当一个指标成为目标,它就不再是好指标。RM 是有限样本的近似,使劲优化就会放大它的误差。
**状态**:✅ 正确。

### 8. RLVR 为什么不需要奖励模型?替代品是什么?
**我的回答**:有些问题本身就有正确答案,1+1 只能等于 2,正确答案本身就是约束。
**批改**:正确。替代品叫**验证器(verifier)**:单元测试、答案匹配、形式化检查器。信号是硬性的,不接受讨价还价。
**状态**:✅ 正确。

### 9. 为什么 RLVR 适合训推理,RLHF 不行?
**我的回答**:正确答案强制模型去思维,形成长思维链。
**批改**:基本对,细节要修——不是验证器"强制"思维,而是模型**自己发现**多步推导、自我检查能提高通过率,思维链是它为拿分自发演化的策略(R1 论文称 "aha moment")。RLHF 做不到,因为偏好信号模糊,且 RM 容易被钻空子。
**状态**:⚠️ 基本正确,表述需精确。

### 10. 一句话:换掉了什么,始终没变的是什么?
**我的回答**:模型结构没调整,只是用不同的答案激活它的能力。
**批改**:❌ **有实质性错误**。前半对,后半错——**权重在每个阶段都被真实更新**:SFT 用梯度下降改权重,RLHF/RLVR 用 RL 改权重,每阶段结束都是一个新模型文件。准确表述:**变的是数据格式与奖励信号,不变的是架构(Transformer)与"预测下一个 token"的引擎**。换的是优化目标,没换的是优化的机器。
**状态**:❌ **需回炉**——"激活"这个比喻只在 LIMA 那一题成立,不能推广到所有阶段。

**第二轮得分:8.5 / 10**

---

## 需回炉清单

1. **BatchNorm 训推不一致**(第一轮第 5 题)——不是过拟合,是统计量来源不同。下一步学习 LayerNorm 前必须搞透
2. **各阶段权重都被真实更新**(第二轮第 10 题)——“激活”仅适用于 SFT 相对预训练,不能套用到 RLHF/RLVR
3. **表述精确化的三处**:test loss 作弊机制、RLVR 思维链的"自发涌现"、base/instruct 的"格式 vs 正确性"

## 复习节奏

- 第 2 周开始前:只看"需回炉清单",答顺就打勾
- 第 4 周复盘时:重做两轮全部 17 题
