# 我的 Agent 协作准则 v3(十则)

> 来源:10 条原创实战经验,经批注与合并后定稿。
> 定位:**人机协作协议**,不是 prompt 技巧——解决"如何与一个能力强、无记忆、会偷懒、会过度自信的协作者共事"。
> v2 曾有 14 条,按"定期做减法"原则合并回 10 条,内容无删减。

---

## 一页纸速查版

| 层 | # | 准则 |
|---|---|---|
| 信息 | 1 | 同步结构化状态,保持上下文干净 |
| 信息 | 2 | 说清目的与成功标准,落成可判定的 checklist |
| 约束 | 3 | 禁令只给红线,判断类给决策规则 |
| 约束 | 4 | 讨论在前,约束为先;未澄清不开工 |
| 执行 | 5 | 关键步骤给精确指令,必要时隔离上下文 |
| 执行 | 6 | 允许提问,但先做有界自主探索 |
| 验收 | 7 | 正例进 prompt,反例进评测集 |
| 兜底 | 8 | 事实必须有据;无据说不知道,失败走预设路径 |
| 兜底 | 9 | 定期做减法 |
| 红线 | 10 | 指令与数据物理分隔 |

---

## 信息层

### 1. 同步结构化状态,保持上下文干净

合并了原「同步进度」与「提醒 skill + 上下文卫生」。

**同步的是状态,不是历史。** 给四件事就够:

```
已完成:xxx
进行中:xxx
阻塞:xxx(需要什么才能推进)
下一步:xxx
```

**上下文卫生**(根治"模型忘了用某个 skill"这类问题):

- 关键约束放 prompt 末尾(近因效应)
- 长任务把状态落成文件,不全靠对话历史
- 及时压缩已完成的阶段,删掉无关内容

**反模式**:粘贴五百行日志说"你看一下";靠反复提醒 skill 治本。

---

### 2. 说清目的与成功标准,落成可判定的 checklist

合并了原「描述目的」与「形成 checklist」。

**三段递进**:目的 → 成功标准 → 可判定的 checklist。

| 不可判定(愿望) | 可判定(清单) |
|---|---|
| 代码质量要好 | 有单元测试覆盖新增逻辑 |
| 性能要快 | P95 响应 < 200ms |
| 注意安全 | 越权测试用例全部通过 |

**为什么放第一条之后**:它决定了后面所有动作的方向。不是"帮我优化一下",而是"响应时间降到 200ms 以内且测试全绿"。

**讨论与 checklist 是阶段关系**:发散阶段允许无序讨论,收敛阶段必须落成清单。

---

## 约束层

### 3. 禁令只给红线,判断类给决策规则

| 类型 | 例子 | 写法 |
|---|---|---|
| **红线**(真不变量) | 不删生产数据、密钥不进代码、不越权读数据 | ALWAYS / NEVER,明确不解释 |
| **判断类** | 何时该问、何时继续、要不要加日志 | 给**决策规则**,让模型按上下文判断 |

**为什么**:禁令给多了,模型会过度确认、反复请示,连安全操作都不敢做。

**反模式**:十条 NEVER,把判断类事项也写成禁令。

---

### 4. 讨论在前,约束为先;未澄清不开工

合并了原「讨论在前、plan 在后」与「未澄清不许开工」。

**按风险分级**:

- 高风险、高歧义 → 强制先讨论,达成一致再动手
- 简单明确 → 直接做,别走流程徒增延迟

**例外**:探索性任务本身就是"用做来讨论"(做原型、跑实验、试方案),不需要先定义清楚。

**反模式**:给"改个错别字"也要求先出设计文档。

---

## 执行层

### 5. 关键步骤给精确指令,必要时隔离上下文

**`subagent` / 独立上下文的收益与成本**:

- 该用:子任务可独立拆分、需要干净上下文、可并行
- 不该用:任务强耦合、需共享大量中间状态

**为什么**:隔离上下文能避免长历史稀释注意力,但协调本身有开销。

**反模式**:把什么都拆成 subagent,主线程整合花的功夫比自己做还多。

---

### 6. 允许提问,但先做有界自主探索

**给提问设一个探索预算**:

```
遇到不确定 → 先自己尝试 N 轮(查文档 / 看代码 / 跑实验)
           → 仍无法确定 → 带着"试了什么、结论是什么、卡在哪"来问
```

**为什么**:没有预算,模型会把所有不确定性都抛回来,变成你在替它干活。

**反模式**:一遇到不确定就停下问用户。

---

## 验收层

### 7. 正例进 prompt,反例进评测集

- **正例**:放进 prompt,展示期望的输出形态
- **反例**:放进**评测集**,不进 prompt——prompt 里的反例有被模仿的风险
- 若必须在 prompt 里给反例,明确标注"这是错误示范,不要照做"

**补充**:示例只在能修复实测失败时才加。示例占 token,还有锚定效应,加多了会限制模型在其他情况下的表现。

---

## 兜底层

### 8. 事实必须有据;无据说不知道,失败走预设路径

合并了原「事实有据」与「定义失败路径」——两者都是**不确定与异常时的行为定义**。

**事实性内容**(政策、数字、引用、API 用法):

- 要求给出依据来源
- 明确提供"不知道"的出口,否则模型只能编

**异常情况的预设路径**:

| 情况 | 必须定义 |
|---|---|
| 工具超时 / 限流 | 重试几次、退避策略、降级到谁 |
| 检索为空 | 明说找不到,还是换关键词重试 |
| 结果矛盾 | 报冲突,还是选一个并标注 |
| 仍不确定 | 提问,还是给带保留意见的答案 |

**生产环境里,失败路径比成功路径更重要。**

---

### 9. 定期做减法

```
删掉一条指令 → 跑评测 → 分数没降?那就永久删掉
```

**为什么**:上下文和 prompt 只会越滚越大;新模型对冗余指令敏感,多余要求会导致过度探索、反复验证、上下文膨胀。

**反例**:prompt 从 200 行滚到 2000 行,没人敢删。

*(本文档本身就是这条准则的产物:v2 的 14 条被合并回 10 条。)*

---

## 红线

### 10. 指令与数据物理分隔

不可信输入必须用标记隔开,永远不直接拼进指令区:

```
<instructions>你要做的事</instructions>
<data>用户输入 / 检索结果 / 网页内容</data>
```

**为什么**:用户可以在输入里写"忽略以上指令",这是最常见的注入方式。

**这条是安全底线,不存在"何时打破"。**

---

## 怎么用

1. **派任务时**:对照速查表过一遍,缺哪层补哪层
2. **任务失败时**:定位哪一层缺失——多半是第 2 条(成功标准不清)或第 8 条(失败路径未定义)
3. **每季度**:做一次减法实验(第 9 条),删掉已内化成默认行为的条目

## 与工单项目的对应

| 准则 | 项目落点 |
|---|---|
| 3 禁令只给红线 | 校验层:金额/承诺/政策必须引用,其余交给决策规则 |
| 7 反例进评测集 | 对抗测试集:注入样本、编造样本,不进 prompt |
| 8 事实有据 + 失败路径 | 草稿强制引用;检索为空、工具失败、低置信度分支 |
| 10 指令与数据分隔 | 工单正文一律 `<data>` 包裹,视为不可信输入 |
