# 我的 Agent 协作准则 v2

> 来源:10 条原创自实际使用经验,经批注与重组后定稿。
> 定位:这是**人机协作协议**,不是 prompt 技巧清单——解决的是"如何与一个能力强、无记忆、会偷懒、会过度自信的协作者共事"。
> 标记:`[原]` = 原创条目;`[改]` = 在原条基础上加限定;`[新]` = 补充维度。

---

## 一页纸速查版

| 层 | 编号 | 准则 |
|---|---|---|
| 信息 | 1 [改] | 同步**结构化状态**,不贴流水账 |
| 信息 | 2 [原] | 说清目的与成功标准 |
| 信息 | 3 [改] | 提醒必要 skill;根治靠上下文卫生 |
| 约束 | 4 [改] | 禁令只给红线,判断类给决策规则 |
| 约束 | 5 [原] | 讨论在前,plan 在后,约束为先,动作在后 |
| 约束 | 6 [原] | 未讨论清楚,不许直接开工 |
| 执行 | 7 [改] | 关键步骤给精确指令,必要时隔离上下文 |
| 执行 | 8 [改] | 允许提问,但先做有界自主探索 |
| 验收 | 9 [改] | 正例进 prompt,反例进评测集 |
| 验收 | 10 [改] | Checklist 必须可判定 |
| 兜底 | 11 [新] | 事实必须有据,无据须说不知道 |
| 兜底 | 12 [新] | 定义失败路径 |
| 兜底 | 13 [新] | 定期做减法 |
| 安全 | 14 [新] | 指令与数据物理分隔 |

---

## 信息层:让模型知道足够多

### 1. 同步结构化状态,不贴流水账 [改]

**原始**:描述背景,或尽可能同步进度,避免模型进度滞后。

**怎么改**:同步的是状态,不是历史。给四件事就够:

```
已完成:xxx
进行中:xxx
阻塞:xxx(需要什么才能推进)
下一步:xxx
```

**为什么**:把历史对话整个贴过去会稀释注意力,模型反而抓不住重点。信息密度比信息量重要。

**反模式**:粘贴五百行日志说"你看一下"。

**何时可省**:新任务、无前置状态时。

---

### 2. 说清目的与成功标准 [原]

**原始**:描述目的,准确形容自己想要的结果。

**补充**:"想要的结果"最好写成**可判定的完成条件**。不是"帮我优化一下",而是"响应时间降到 200ms 以内且测试全绿"。

**这是最核心的一条**——它决定了后面所有动作的方向。

**反模式**:只描述动作("帮我改改这个函数"),不描述目标。

---

### 3. 提醒必要 skill;根治靠上下文卫生 [改]

**原始**:提醒 skill,避免模型因上下文太长忘记使用必要的 skill。

**怎么改**:提醒是**权宜之计**,根治方法是上下文卫生:

- 关键约束放在 prompt 末尾(近因效应)
- 长任务把状态落成文件,而不是全靠对话历史
- 及时压缩已完成的阶段
- 删掉与当前任务无关的上下文

**为什么**:上下文稀释(context dilution)是真实存在的——信息越多,每条信息的权重越低。

---

## 约束层:划清边界

### 4. 禁令只给红线,判断类给决策规则 [改]

**原始**:描述禁令,避免模型触碰禁区。

**怎么改**:区分两类约束:

| 类型 | 例子 | 写法 |
|---|---|---|
| **红线**(真不变量) | 不许删生产数据、不许把密钥写进代码、不许越权读数据 | 用 ALWAYS / NEVER,明确且不解释 |
| **判断类** | 什么时候该问、什么时候该继续、要不要加日志 | 给**决策规则**,让模型按上下文判断 |

**为什么**:禁令给多了,模型会过度确认、反复请示,连安全操作都不敢做。

**反模式**:十条 NEVER,把判断类事项也写成禁令。

---

### 5. 讨论在前,plan 在后,约束为先,动作在后 [原]

**原始**:同上。

**补充**:按**风险/歧义度分级**执行:

- 高风险、高歧义 → 强制先讨论,达成一致再动手
- 简单明确 → 直接做,不必走流程,否则徒增延迟

**反模式**:给"改个错别字"也要求先出一份设计文档。

---

### 6. 未讨论清楚,不许直接开工 [原]

**原始**:不允许在未讨论清楚的前提下直接开始工作,除非工作就是发散性讨论。

**例外**:探索性任务本身就是"用做来讨论"——做原型、跑实验、试方案都属于发散,不需要先定义清楚。

---

## 执行层:控制过程

### 7. 关键步骤给精确指令,必要时隔离上下文 [改]

**原始**:在关键步骤给出准确的指令,例如开一个 subagent 给出干净的上下文。

**补充**:`subagent` 的收益是**上下文隔离 + 并行**,成本是**协调开销**。

- 该用:子任务可独立拆分、需要干净上下文、可以并行
- 不该用:任务强耦合、需要共享大量中间状态

**反模式**:把什么都拆成 subagent,结果主线程在整合上花的时间比自己做还多。

---

### 8. 允许提问,但先做有界自主探索 [改]

**原始**:可以迷茫,如果在不了解的地方碰壁,允许提问。

**怎么改**:给出**提问前的探索预算**:

```
遇到不确定 → 先自己尝试 N 轮(查文档/看代码/跑实验)
           → 仍无法确定 → 带着"我试了什么、结论是什么、卡在哪"来问
```

**为什么**:没有探索预算,模型会把所有不确定性都抛回来,变成你在替它干活。

**反模式**:一遇到不确定就停下问用户。

---

## 验收层:收口

### 9. 正例进 prompt,反例进评测集 [改]

**原始**:给出例子,正例和反例。

**怎么改**:

- **正例**:放进 prompt,展示期望的输出形态
- **反例**:放进**评测集**,而不是 prompt——放进 prompt 的反例有被模仿的风险
- 若必须在 prompt 里给反例,明确标注"这是错误示范,不要照做"

**补充**:示例只在能修复实测失败时才加。示例占 token,且有锚定效应,加多了会限制模型在其他情况下的表现。

---

### 10. Checklist 必须可判定 [改]

**原始**:形成 checklist 比单纯的讨论更重要。

**怎么改**:每一条都必须能被判定"过 / 不过"。

| 不可判定(愿望) | 可判定(清单) |
|---|---|
| 代码质量要好 | 有单元测试覆盖新增逻辑 |
| 性能要快 | P95 响应 < 200ms |
| 注意安全 | 越权测试用例全部通过 |

**补充**:讨论和 checklist 不是替代关系,而是**阶段关系**——发散阶段允许无序讨论,收敛阶段必须落成 checklist。

---

## 兜底层:新增的四个维度

### 11. 事实必须有据,无据须说不知道 [新]

涉及事实性内容(政策、数字、引用、API 用法)时:

- 要求给出依据来源
- 明确提供"不知道"的出口,否则模型只能编
- 检索为空时走"未找到依据"分支,而不是硬答

**反模式**:只要求"准确回答",不给认输的路径。

---

### 12. 定义失败路径 [新]

不只描述理想流程,还要写清异常时怎么办:

| 情况 | 必须定义 |
|---|---|
| 工具超时 / 限流 | 重试几次、退避策略、降级到谁 |
| 检索为空 | 明说找不到,还是换关键词重试 |
| 结果矛盾 | 报冲突,还是选一个并标注 |
| 不确定 | 提问,还是给带保留意见的答案 |

**生产环境里,失败路径比成功路径更重要。**

---

### 13. 定期做减法 [新]

上下文和 prompt 只会越滚越大。定期做减法实验:

```
删掉一条指令 → 跑评测 → 分数没降?那就永久删掉
```

**为什么**:新模型对冗余指令敏感,多余的要求会导致过度探索、反复验证、上下文膨胀。

**反例**:prompt 从 200 行滚到 2000 行,没人敢删。

---

### 14. 指令与数据物理分隔 [新]

不可信输入必须用标记隔开,永远不直接拼进指令区:

```
<instructions>你要做的事</instructions>
<data>用户输入 / 检索结果 / 网页内容</data>
```

**为什么**:用户可以在输入里写"忽略以上指令",这是最常见的注入方式。**这条是安全底线,不存在"何时打破"。**

---

## 怎么用这份准则

1. **写 prompt / 派任务时**:对照"一页纸速查版"过一遍,缺哪层补哪层
2. **任务失败时**:定位是哪一层缺失——多半是信息层的成功标准没写清,或兜底层没定义
3. **每季度**:做一次减法实验(第 13 条),删掉已经内化成默认行为的条目

## 与工单项目的对应

| 准则 | 在工单项目里的落点 |
|---|---|
| 4 禁令只给红线 | 校验层:金额/承诺/政策必须引用,其余交给决策规则 |
| 9 反例进评测集 | 对抗测试集:注入样本、编造样本,不进 prompt |
| 11 事实有据 | 草稿强制引用,校验层逐条比对检索证据 |
| 12 失败路径 | 检索为空、工具失败、低置信度的分支定义 |
| 14 指令与数据分隔 | 工单正文一律用 `<data>` 包裹,视为不可信输入 |
