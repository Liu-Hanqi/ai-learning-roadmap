# 文本建模基础复习笔记(bigram → MLP)

> 写给有 CV 背景的自己:文本建模的核心机制其实都见过,只是输入输出的形态变了。
> 主线:统计方法 → 单字符 NN → embedding → 多字符 MLP。

---

## 0. 语言模型到底在干什么

一句话:**给定前面的内容,预测下一个 token 的概率分布**。

- 训练目标:让真实下一个 token 的概率尽量大,即最小化负对数似然(NLL)
- 损失函数:交叉熵。就是 CV 里分类任务的 softmax + cross entropy,一字未变
- 区别只在输出层含义:CV 里分类头是"这是哪一类图片",LM 里分类头是"下一个词是哪一个",词表有多大,类别就有多少(几万类很正常)

生成文本 = 反复采样:预测分布 → 采一个 token → 接回输入 → 再预测。和扩散模型一步步去噪在"迭代生成"这个感觉上很像。

---

## 1. 统计方法:n-gram

### 做法(bigram 为例)

1. 遍历语料,统计"字符 i 后面跟字符 j"的次数,得到 27×27 计数表
2. 每行归一化 → 条件概率 P(下一个 | 当前)
3. 预测/采样直接查表

### 为什么它是对的

在"只看前一个字符"这个前提下,频率就是最大似然估计,统计上无可指摘。

### 为什么它走不远

- **组合爆炸**:上下文长度 n 每加 1,表的大小乘 27。trigram 是 27³ ≈ 2 万,10 个字符就是天文数字
- **零泛化**:没见过的组合概率为零(或靠 smoothing 硬编)。"the cat sat" 见过不能帮 "the dog sat"

> CV 类比:像用直方图查表做分类,不做任何学习。类别一多就崩。

---

## 2. NN 版 bigram:一层网络重造计数表

### 流水线(整条后面都要复用)

```
字符 → one-hot(27 维) → @ W(27×27) → logits → softmax → 概率
                                                          ↓
                                              cross_entropy(概率, 真实下一个字符)
                                                          ↓
                                              backward → 梯度下降更新 W
```

### 三个关键点

1. **one-hot @ W = 取 W 的第 i 行**。所以 W 的第 i 行就是"第 i 个字符后面各字符的打分",矩阵乘法在这里只是查表的数学写法
2. **交叉熵 = 负对数似然**。对正确类别的概率取 -log,和统计方法优化的目标完全一致
3. **训练收敛后,exp(W) ≈ 计数表**。梯度下降重新发现了数数的结果——两种方法等价

### 既然等价,为什么还要用 NN?

因为 NN 框架**可扩展**:加上下文、加层数、换 embedding,流水线不变。统计方法走到 bigram 就是尽头,NN 才刚开始。这一课练的是肌肉记忆,不是结果。

---

## 3. Embedding:从孤立符号到语义空间

### one-hot 的缺陷

任意两个 token 的 one-hot 向量都正交:距离一样、点积为零。**模型眼里所有词彼此一样陌生**,词与词没有任何关系可言。

### embedding 的做法

把 27 维 one-hot 压成 d 维稠密向量(d 可以很小,课上用 2):

- 实现:一个 27×d 的矩阵 C,查表即 `C[i]`,等价于 one-hot @ C
- C 随机初始化,**跟着整个网络一起被梯度下降训练**

### 语义是怎么"长"出来的

机制:出现在相似上下文的 token(如元音 a/e/i),对预测的贡献接近,网络为了降损会把它们的向量往同方向调。训完之后:

- 用法相近的 token → 向量靠得近(课上 2 维散点图里元音聚成一团)
- **空间结构 = 语义结构**

### 两个重要细节

1. embedding 编码的是**用法分布的相似**,不是含义的相似。"好"和"坏"上下文几乎相同,向量很近——静态 embedding 分不出反义词,要靠上层网络结合上下文
2. 方向也有语义:king - man + woman ≈ queen,偏移向量 ≈ "性别"维度

### 为什么这解决了 n-gram 的死穴

**泛化**。cat 和 dog 向量近,"the cat sat" 学到的知识自动惠及 "the dog sat"。没见过的组合也能借语义相似性猜个八九不离十。

> CV 类比:one-hot 像 class id,embedding 像 backbone 倒数第二层的特征向量——特征空间里相似的类聚在一起,还能做迁移。只不过这里特征是可学习的输入端。

---

## 4. MLP 语言模型(Bengio 2003)

### 结构

```
前 n 个字符 (例:n=3, "e", "m", "m")
   ↓ 各自查 embedding 表 C,得到 n 个 d 维向量
拼接成 n*d 维长向量
   ↓ 线性层 + tanh(隐藏层,几十到几百维)
   ↓ 线性层(输出 27 维 logits)
softmax → 下一个字符的概率
```

### 相比 bigram 多了什么

1. **上下文变长**:一次看 3 个字符而不是 1 个,预测依据丰富得多
2. **embedding 压缩**:n 个字符共享一张表 C,参数量可控(这是 Bengio 论文的核心贡献——用共享稠密表示对抗维度灾难)
3. **隐藏层做非线性组合**:tanh 层能把"字符 A 后面跟 B 且再后面常跟 C"这类高阶模式揉出来

### 参数量直觉

主要参数在隐藏层:n*d × h + h × 27。C 只占 27×d。加大 d 或 h 都能提升容量,loss 稳定下降——这就是"scaling"的迷你版预演。

---

## 5. 训练环节的实战坑(这几课最工程的部分)

### 初始化

- **hockey stick loss**:开局 loss 骤降后平缓。病因是初始 logits 太离谱,softmax 输出接近 one-hot,前几步全在纠这个。解法:输出层权重初始化乘 0.01,bias 置零,让初始分布接近均匀(loss ≈ ln 27 ≈ 3.3)
- **dead tanh**:隐藏层 pre-activation 太大,tanh 饱和(输出 ±1),梯度归零,神经元"死亡"。诊断:h 取整后画直方图,一大片 ±1 就是病了。解法:初始化增益按 5/3/√fan_in(kaiming 的思想,tanh 的增益是 5/3)

### BatchNorm

- 作用:把 pre-activation 归一到均值 0 方差 1,再加可学习的 scale/shift(γ/β)。网络不再对初始化和学习率那么敏感
- 坑:**训练和推理行为不一致**(训练用 batch 统计,推理用全量估计的 running 统计)。makemore 里名字生成变成"复读机"就是这个 bug
- 副作用:batch 内样本互相耦合,loss 有轻微正则效果,但也让结果依赖 batch 构成

### 训练流程规范

1. **先过拟合一个小 batch**:loss 能打到接近 0,说明代码能学;打不下去先查 bug 再谈调参
2. **学习率搜索**:试 0.001 到 1 的数量级,画 lr-loss 曲线找拐点;训练中后期 decay(如 ÷10)
3. **train / dev / test 三分**:dev 用来调超参,test 全程只碰少数几次。反复看 test = 在 test 上过拟合

> CV 类比:这些和训 CNN 的坑完全同源——初始化、归一化、lr schedule、数据泄漏,一套纪律两处通用。

---

## 6. 一张图总览演进路线

```
n-gram 计数表        统计,零泛化,上下文一长就爆炸
    ↓ 换成可学习参数
NN bigram           one-hot → 线性 → softmax,等价于计数表,但框架可扩展
    ↓ 输入换成稠密向量
embedding           符号有了空间坐标,泛化出现(用法相似 → 距离近)
    ↓ 上下文加长 + 加隐藏层
MLP(Bengio 2003)   共享 embedding + 非线性组合,mini 版 scaling
    ↓ 更长上下文
RNN/LSTM            用循环状态携带任意长历史(有遗忘瓶颈)
    ↓ 抛弃循环,全量注意力
Transformer         attention 直接建模任意位置间关系,GPT 的起点
```

每一步只改一件事,其余流水线(one-hot/embedding → 网络 → softmax → 交叉熵 → 反向传播)原封不动。**这条流水线就是全部**。

---

## 7. 快速自测(能答出来才算真懂)

1. 为什么 exp(W) 会收敛到 bigram 计数表?
2. one-hot 乘矩阵为什么等价于查表?这个等价关系在 embedding 层怎么用?
3. embedding 里"好"和"坏"为什么离得近?这暴露了静态 embedding 的什么局限?
4. tanh 死亡怎么诊断、怎么治?
5. BatchNorm 训练和推理为什么不一致?会引起什么实际 bug?
6. 为什么反复看 test loss 等于作弊?
7. 从 MLP 到 Transformer,被换掉的核心组件是什么?没换的是什么?
