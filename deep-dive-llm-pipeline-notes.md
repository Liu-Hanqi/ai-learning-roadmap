# Deep Dive:预训练 → SFT → RLHF/RLVR 全流程笔记

> 对应 Karpathy《Deep Dive into LLMs like ChatGPT》。
> 重点放在 CV 里没有对应物的三块:**tokenizer 工程细节、SFT 数据构造、RLHF 到 RLVR 的演进**。
> 其余部分(预训练目标、梯度下降)只做最小必要回顾。

---

## 0. 全局地图:一个模型是怎么"长大"的

```
阶段          数据                     目标函数                产物
─────────────────────────────────────────────────────────────────────
预训练        万亿级 token 网页/代码    next-token 交叉熵       base model(文本模拟器)
   ↓
SFT          万~百万条对话示范         next-token(只在回复上)  instruct model(会扮演助手)
   ↓
RLHF         人类偏好排序              RL + 奖励模型 - KL       aligned model(符合人类偏好)
   ↓
RLVR         有客观对错的问题          RL + 可验证奖励          reasoning model(会推理)
```

一句话概括:预训练让模型**会说话**,SFT 让它**会说人话**,RLHF 让它**说得讨人喜欢**,RLVR 让它**说得对**。

---

## 1. 预训练(快速回顾)

- **目标**:给定前文,预测下一个 token。全部秘密就这一条
- **数据**:Common Crawl 网页、书籍、论文、代码仓库,清洗后几万亿 token。数据质量和配比(filtering, 去重, 领域混比)对最终能力的影响,往往比架构调整更大
- **训练**:就是上一课那条流水线,放大几万倍——几千张 GPU 跑几个月
- **产物:base model**。关键认知:base model 是一个**互联网文本的统计模拟器**,不是助手。你问它"法国首都是哪",它可能接着写"德国首都是哪,意大利首都是哪"——因为它在模拟一份"问题清单"网页,而不是回答问题。**它的损失函数只奖励"预测得像",不奖励"有用"或"正确"**

> CV 类比:这一步相当于在超大规模数据上自监督预训练一个通用 backbone(MAE/BYOL 那条线)。你拿到的是通用特征,不是能直接用的产品。

---

## 2. Tokenizer:文本如何变成整数(重点一)

### 2.1 为什么不用字符,也不直接用词

| 方案 | 词表大小 | 问题 |
|---|---|---|
| 字符级 | 几百 | 序列太长,attention 成本 T² 吃不消;每个字符信息量太低 |
| 词级 | 几十万~百万,OOV 无解 | 词形变化、新词、拼写错误全部抓瞎;词表爆炸 |
| **子词(subword)** | 几万 | 折中:常用词整块存,罕见词拆成片段 |

### 2.2 BPE(Byte Pair Encoding)算法

核心思想:**从最小的单位开始,统计频率,贪心地合并**。

```
1. 初始化:把所有文本拆成字节(256 个基础符号)
2. 统计所有相邻符号对的出现频率
3. 合并频率最高的一对,加入词表
4. 重复 2-3,直到词表达到目标大小(如 50000 次合并)
```

合并规则表就是 tokenizer 本身。编码新文本时,按学到的规则依次应用。

**举例**:"lower" 可能出现 1000 次,"lower" 里的 (l,o)、(o,w)、(w,e)、(e,r) 各自高频,于是被合并成 "lo"、"low"、"lowe"、"lower",最终 "lower" 可能是一个 token,而 "lowest" 被切成 "low" + "est"。

### 2.3 为什么是 50257 这种怪数字

GPT-2 的词表构成(精确拆解):

```
256   个基础字节(byte-level,保证任何文本都能编码,没有 OOV)
50000 个 BPE 合并出的 token
1     个特殊 token <|endoftext|>(文档分隔符)
─────
50257 总量
```

它不是 2 的幂,纯粹是这些数字加出来的。而 nanoGPT 里你会看到 `vocab_size=50304`,那是 GPT-2 的 50257 **向上 padding 到 64 的倍数**——纯粹为了 GPU 矩阵乘的整除效率,多出来的位置空着不用。

### 2.4 工程上必须知道的细节

1. **token 不等于字符,也不等于词**。一个汉字可能 1~3 个 token,一个英文长词可能被切好几块。上下文长度、计费、API 限速,全都按 token 算
2. **数字分词很反直觉**:"1234" 的分词和 "1235" 完全无关,模型做算术时看到的是不规律的数字碎片——这是 LLM 算术能力弱的一个隐藏原因
3. **前导空格被合并**:" world" 和 "world" 是不同的 token。这也是为什么提示词里多余的空格有时会改变输出
4. **不同模型的 tokenizer 不通用**。换模型必须换 tokenizer,跨模型比较"上下文长度"时也要换算
5. **中文效率偏低**:中文平均 1~2 个 token/字,同样内容比英文占更多 token。看模型中文能力时,这直接影响成本和有效上下文

> 动手验证:`pip install tiktoken`,用 GPT-2 的编码器把一句话编码出来数一数,亲眼看分块方式。半小时的实验胜过读十篇文章。

---

## 3. SFT:让模型学会"对话"这个格式(重点二)

### 3.1 base model 的问题

base model 只会**续写**。你输入 "中国的首都是",它续写 "北京。美国的首都是华盛顿。"——它把一个问句当成了网页里的一段文字,继续补全。

### 3.2 核心手段:把对话伪装成文本

SFT 的全部魔法就一句话:**把对话数据模板化成一段普通文本,然后用和预训练完全一样的目标去训练**。

```
<|im_start|>system
你是一个乐于助人的助手<|im_end|>
<|im_start|>user
中国的首都是哪里?<|im_end|>
<|im_start|>assistant
北京。<|im_end|>
```

模型看到的仍然只是"预测下一个 token",只不过这堆文本恰好长得像对话。训练几千~几万条这样的样本后,模型学会了这个格式的模式:见到 "assistant" 之后,就该输出回答。

### 3.3 一个关键工程细节:loss 只算在回复上

对话里有 system、user、assistant 三种内容。**通常只对 assistant 部分计算损失**(其余位置 mask 掉)。

为什么?因为我们的目标是让模型学会"回答",不是学会"提问"。如果把用户的话也拿来训练,模型会花一部分容量去模仿提问的措辞,没有任何好处。

> 这份"哪些 token 算 loss"的掩码,就是各家 chat template 的实现差异之一。用错模板会让效果明显变差——这也解释了为什么下游工具(如 vLLM、Ollama)要严格匹配模型的 template。

### 3.4 数据量与质量

- 早期以为要百万条,后来 **LIMA 论文**证明:1000 条精心挑选的高质量对话,就能让模型表现出很强的指令跟随能力
- 结论:**SFT 是在"激活"预训练里已有的能力,不是"注入"新能力**。预训练没学过的知识,SFT 补不回来
- 训练量小:通常 1~3 个 epoch,学习率远低于预训练,跑几天就够

### 3.5 SFT 的局限(理解幻觉的来源)

SFT 本质上还是"模仿示范"。它教会模型"答案长什么样",但没有机制判断"答案对不对"。模型如果不知道答案,最符合损失函数的行为就是**生成一个看起来像正确答案的东西**——幻觉的种子在这里就被埋下了。

> CV 类比:SFT 就是标准的有监督微调。预训练是无标注通用特征,现在拿一份标注好的数据集(对话示范)去 finetune。唯一的新东西是"数据被序列化成了特定格式"。

---

## 4. RLHF:用人类偏好来优化(重点三)

### 4.1 为什么要跳过 SFT

SFT 只能模仿**一条**示范。但很多问题没有唯一正确答案,只有"更好"和"更差":简洁还是详细?保守还是有趣?人类标注员在没有标准答案时,能给出的是**比较和排序**,不是标准答案。

RLHF 的价值就在于:**它能把"哪个更好"这种弱信号利用起来,而不需要一个完美的标准答案**。

### 4.2 三步流程

**第一步:收集偏好数据**
给模型同一个问题生成多个回答,让人类标注员排序(A > B > C)。

**第二步:训练奖励模型(Reward Model)**
用排序数据训练一个打分器:输入 (prompt, response),输出一个标量分数。

- 训练目标是让"人类更喜欢的回答"得分更高(pairwise ranking loss, 类似 Bradley-Terry 模型)
- 本质:把"人类偏好"蒸馏成一个可微的神经网络——之后就不需要人每次都参与

**第三步:用 RL 优化策略**
用 PPO 之类的算法调整模型,让奖励模型的打分更高:

```
maximize  E[r(x, y)] − β · KL(π || π_ref)
            奖励分数        惩罚:不许偏离 SFT 模型太远
```

### 4.3 为什么必须有 KL 惩罚

如果只最大化奖励分数,模型会找到奖励模型的漏洞——生成一些**人类会觉得荒谬、但奖励模型打高分**的内容。这就是 **reward hacking**。KL 惩罚像一根弹力绳,把模型拴在 SFT 模型附近,防止它为了刷分跑偏到无意义的区域。

> CV 类比:奖励模型相当于学一个"人类偏好打分器",类似 CLIP 学图文匹配分数、RankNet 学排序。用这个可微的打分器去反传优化生成模型,是 NLP 里比较独特的用法。

### 4.4 RLHF 的固有缺陷

1. **标注贵、慢、主观**——需要雇佣和培训人类标注员,一致性难保证
2. **奖励模型只是代理**——它逼近人类偏好,但永远不等价,优化它就会暴露它的漏洞
3. **讨好倾向**——人类偏好"说得自信、说得委婉",模型就学成爱拍马屁,而非坚持正确答案
4. **不可验证领域才有必要**——如果答案本身就能自动判断对错,何必请人来打分?

最后一条直接催生了 RLVR。

---

## 5. RLVR:用可验证奖励做强化学习

### 5.1 核心思想

有些任务的**正确性可以被程序自动验证**:

- 数学题:答案对不上参考答案,或者符号计算器验证不过
- 代码题:跑单元测试,通过 = 1 分,失败 = 0 分
- 形式逻辑/证明:证明检查器
- 选择题/填空题:精确匹配

这类任务不需要人类打分,也不需要训练奖励模型——**直接用规则给出奖励信号**。

### 5.2 训练方式

```
对每个问题:
  模型生成 N 个回答
  自动验证器给每个回答打分(0/1,或按测试通过率)
  用 RL 提升高分回答的概率、压低低分回答
```

代表算法是 **GRPO**(Group Relative Policy Optimization):DeepSeek-R1 用的就是它。相比 PPO,它不需要单独的价值网络,直接用同一组回答的相对好坏作为基线,更省资源。

### 5.3 为什么它引爆了推理模型

- **奖励信号是可信的**。RLHF 里模型可以讨好奖励模型,但单元测试不会拍马屁——要么过要么不过
- **数据近乎无限**。数学题可以自动生成、自动验证,不用人类标注
- **训练中涌现出长思维链**。模型发现"想得更久、多步检查"能提高通过率,于是自发学会了"先列条件、再推导、最后验算"的模式(R1 论文里称之为 "aha moment")
- **自我验证与自我纠错**变成可学的行为

### 5.4 局限

1. **必须有验证器**。写诗、谈心、开放性创作——你说什么算对?没法自动验证,这类领域 RLVR 无能为力
2. **奖励可以作弊**。代码题若测试用例覆盖不全,模型会学会"针对测试用例硬编码";数学若只对答案,会学会"数字碰运气"
3. **域外泛化存疑**。在数学题上练出的推理能力,能迁移到多少其他任务,仍是开放问题
4. **并非替代 RLHF**。现代后训练配方通常是:**SFT 打底 + RLHF 学对齐 + RLVR 练推理**,三者混合使用

---

## 6. 全景对比表

| 阶段 | 数据 | 目标函数 | 需要什么额外组件 | 产物 |
|---|---|---|---|---|
| 预训练 | 万亿 token 原始文本 | next-token CE | 海量算力 | base model |
| SFT | 万~百万条对话示范 | next-token CE(只看回复) | chat template | instruct model |
| RLHF | 人类偏好排序 | RL + RM 分数 − KL | 奖励模型 + 人类标注 | aligned model |
| RLVR | 有客观正误的问题 | RL + 规则奖励 | 验证器(测试/答案) | reasoning model |

**记住了这三条,整条线就通了:**

1. 每一步都没有换掉那台引擎——**都是"预测下一个 token"**,变的是数据格式和奖励信号
2. 信息注入方式越往后越"弱":示范是强监督,偏好排序是弱监督,奖励信号只给一个数字——但**能利用的数据规模和类型越来越广**
3. 幻觉、讨好、推理能力,全都是各自阶段目标函数的直接产物——理解目标函数,就理解了模型的行为怪癖

---

## 7. 自测题

1. base model 和 instruct model 的本质区别是什么?为什么 base model 会"答非所问"?
2. BPE 的 50000 次合并是怎么决定的?为什么词表是 50257 而不是整数?
3. 为什么字节级编码能保证不存在 OOV?代价是什么?
4. SFT 里为什么通常只对 assistant 部分算 loss?如果把 user 部分也算上会怎样?
5. LIMA 的 1000 条结论说明了 SFT 在做什么(注入还是激活)?
6. 为什么 RLHF 需要 KL 惩罚?没有它会发生什么?
7. 奖励模型和人类偏好是什么关系?为什么说它只是个"代理"?
8. RLVR 为什么不需要奖励模型?它需要什么替代品?
9. 为什么 RLVR 特别适合训练推理能力,而 RLHF 不行?
10. 用一句话说清:从预训练到 RLVR,被换掉的是什么,始终没变的是什么?

---

## 8. 延伸阅读(按重要性排序)

- InstructGPT(Ouyang et al., 2022)——RLHF 三阶段的标准范式
- LIMA(Zhou et al., 2023)——"少而精"的 SFT 证据
- DeepSeek-R1(2025)——GRPO + RLVR 训练推理模型的公开细节
- GPT-2 论文的 tokenizer 部分 / tiktoken 源码——BPE 的工程实现
